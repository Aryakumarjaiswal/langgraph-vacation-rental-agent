apiVersion: v1
kind: Service
metadata:
  name: stayops-api
  labels:
    app: stayops-api
spec:
  type: ClusterIP
  selector:
    app: stayops-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
