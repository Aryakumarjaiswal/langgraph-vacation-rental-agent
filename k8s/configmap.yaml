apiVersion: v1
kind: ConfigMap
metadata:
  name: stayops-config
  labels:
    app: stayops
data:
  DB_HOST: "mysql"
  DB_PORT: "3306"
  DB_NAME: "Conversations"
  DB_USER: "root"
  GEMINI_MODEL: "gemini-2.0-flash"
  EMBEDDING_MODEL: "BAAI/bge-small-en-v1.5"
  CHROMA_PATH: "chroma_db/UNITS_INFO_CHUNCK"
  FINAL_DATA_PATH: "data/final_data.xlsx"
  # Streamlit pods call the API service inside the cluster.
  API_BASE_URL: "http://stayops-api:8000"
